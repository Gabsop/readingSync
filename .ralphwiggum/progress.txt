# ReadingSync Mobile App — Progress

## M1 — Foundation

### Completed
- [x] Project scaffolding (Expo SDK 54 + TypeScript) — `mobile/` directory
  - Expo blank-typescript template
  - expo-router for file-based navigation
  - Core dependencies: zustand, expo-sqlite, expo-file-system, expo-secure-store,
    react-native-reanimated, react-native-gesture-handler, expo-auth-session,
    expo-haptics, expo-blur, expo-brightness, @react-navigation/*
  - TypeScript strict mode, passes type check
- [x] Navigation structure (Login -> Library -> Reader)
  - Root layout with Stack navigator (expo-router)
  - Tab layout: Reading Now, Library, Search (with Ionicons)
  - Login screen with Google sign-in placeholder
  - Reader screen with dynamic [bookId] route
  - Placeholder screens for all tabs with empty states

### Not Started

### Completed (continued 3)
- [x] Mobile login screen: wired up expo-auth-session to better-auth Google OAuth
  - Auth store (`mobile/lib/auth-store.ts`): Zustand + expo-secure-store for token persistence
  - API helper (`mobile/lib/api.ts`): backend URL config + Bearer token–injecting `authFetch`
  - Mobile callback endpoint (`src/app/api/auth/mobile-callback/route.ts`): reads better-auth
    session cookie after OAuth, redirects to `readingsync://auth-callback?token=<token>`
  - Login screen opens better-auth sign-in URL via `expo-web-browser` `openAuthSessionAsync`;
    Google OAuth → better-auth callback → mobile-callback → app redirect with token
  - Index screen loads token from SecureStore, redirects to library if authenticated
  - Open redirect protection: mobile-callback validates redirect_uri against allowed schemes
  - Loading + error states on sign-in button

### Completed (continued 2)
- [x] better-auth setup on backend (replaces NextAuth + Discord OAuth)
  - Installed `better-auth`, removed `next-auth` and `@auth/drizzle-adapter`
  - Configured Google OAuth provider via `betterAuth()` with Drizzle adapter
  - New auth schema: `web_users`, `web_accounts`, `web_sessions`, `web_verifications`
  - Catch-all route handler at `/api/auth/[...all]` via `toNextJsHandler`
  - Auth client export (`src/lib/auth-client.ts`) for React components
  - Session helper (`src/server/auth/helpers.ts`) for REST API route protection
  - tRPC context updated to use `auth.api.getSession()` with headers
  - Env vars updated: `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`, `BETTER_AUTH_URL`, `BETTER_AUTH_TRUSTED_ORIGINS`
  - Drizzle migration generated, TypeScript passes cleanly

### Completed (continued 5)
- [x] Backend: add `device_id` field to progress records
  - Added nullable `deviceId` (varchar 64) column to `readingProgress` and `syncHistory` tables
  - REST `POST /api/progress` accepts `device_id` in request body, stores in both tables
  - REST `GET /api/progress/:bookId` returns `device_id` in response (both direct and source-filtered)
  - tRPC `save` mutation accepts optional `deviceId` input, stores in both tables
  - Drizzle migration generated (`0002_violet_hex.sql`)
  - Enables same-device detection for sync conflict resolution
  - TypeScript passes cleanly

### Completed (continued 4)
- [x] API key generation endpoint for KOReader plugin
  - New `web_api_keys` table: userId (FK), prefix, keyHash (SHA-256, unique), name, lastUsedAt, createdAt, revokedAt
  - Drizzle migration generated (`0001_fearless_thor_girl.sql`)
  - `POST /api/auth/api-keys` — generates a new API key (returns full key once, stores SHA-256 hash)
  - `GET /api/auth/api-keys` — lists active keys for the authenticated user (prefix, name, lastUsedAt)
  - `DELETE /api/auth/api-keys` — soft-revokes a key by setting revokedAt
  - `getSessionFromRequest()` now resolves `x-api-key` header: hashes the incoming key, looks up in
    `web_api_keys` (excluding revoked), joins to `web_users`, updates `lastUsedAt` fire-and-forget
  - Key format: `rs_k_<32 random bytes base64url>`, prefix stored as first 13 chars for display
  - Utility module (`src/server/auth/api-key-utils.ts`): `generateApiKey()` and `hashApiKey()`
  - TypeScript passes cleanly

### Completed (continued)
- [x] EPUB parser module (`mobile/lib/epub-parser.ts`)
  - Reads .epub (zip) via expo-file-system `File.arrayBuffer()` + JSZip
  - Parses META-INF/container.xml → locates OPF rootfile
  - Parses OPF → extracts metadata (title, author, cover, language, etc.)
  - Builds manifest map (id → href, mediaType, properties)
  - Extracts spine reading order with resolved hrefs from manifest
  - Parses TOC: EPUB 3 nav document and EPUB 2 NCX fallback
  - Cover image extraction (EPUB 3 properties, EPUB 2 meta, id heuristic)
  - `readChapter()` helper for accessing chapter XHTML by spine index
  - Dependencies added: jszip, fast-xml-parser
- [x] Basic XHTML → React Native component mapping (`mobile/lib/xhtml-renderer.tsx`)
  - Renders EPUB XHTML as native RN components (Text, View, Image)
  - Supports: p, h1–h6, em/i, strong/b, u, s/del, sup/sub, a, span
  - Supports: ul/ol/li lists, blockquote, hr, br, img, basic tables
  - Inline CSS style parsing (font-weight, font-style, text-align, color, font-size, margins)
  - Reader screen wired up: loads book from SQLite, parses EPUB, renders chapters
  - Chapter-by-chapter navigation (prev/next) with loading/error states
- [x] Local SQLite database schema
  - Tables: books, reading_positions, sync_queue, settings
  - WAL journal mode, foreign keys enabled
  - Schema versioning via PRAGMA user_version for future migrations
  - SQLiteProvider wired into root layout with Suspense
  - Database accessible via `useSQLiteContext()` hook from all screens

## M2 — Reading Experience

### Completed
- [x] Pagination engine with accurate text measurement
  - Paginated ScrollView with `snapToInterval` based on available screen height
  - Content height measured via `onContentSizeChange`, page count = ceil(contentHeight / pageHeight)
  - Tap-zone navigation: left 40% = previous page, right 40% = next page (Apple Books pattern)
  - Center 20% reserved for controls overlay toggle (future task)
  - Automatic chapter transitions: last page → next chapter, first page back → previous chapter
  - Global page numbering across chapters (e.g., "15 of 561")
  - Chapter page counts cached in ref for cross-chapter page calculation
  - EPUB parsed once and cached in ref (avoids re-parsing on chapter change)
  - `ParsedEpub` type exported from epub-parser for use in reader screen
  - `PaginatedChapter` reusable component created (`mobile/lib/paginated-view.tsx`)
  - Page indicator in footer (replaces old chapter-level prev/next buttons)
  - TypeScript passes cleanly

### Completed (continued)
- [x] Page turn gestures + slide animation
  - Horizontal pan gesture via `react-native-gesture-handler` Gesture.Pan()
  - Finger-tracked slide animation using `react-native-reanimated` shared values
  - Swipe triggers at 25% of screen width or 500px/s velocity (fast flick)
  - Slide-out animation (250ms, cubic ease-out) before page change
  - Spring snap-back on cancelled swipe (damping 20, stiffness 300)
  - Subtle shadow overlay grows with swipe distance for depth effect
  - Tap zones preserved: left 40% = prev, right 40% = next, center 20% = controls (future)
  - Gesture composition: `Gesture.Race(pan, tap)` — pan wins if horizontal, tap wins otherwise
  - Pan activates after 15px horizontal movement, cancels on >10px vertical (no scroll conflict)
  - Light haptic feedback (`expo-haptics` ImpactFeedbackStyle.Light) on every page turn
  - Animation lock prevents double-triggers during slide-out
  - TypeScript passes cleanly

### Completed (continued 2)
- [x] Reader settings panel (font, size, theme)
  - Settings store (`mobile/lib/reader-settings.ts`): types, theme definitions, defaults, SQLite persistence
  - 6 Apple Books themes: Original, Quiet, Paper, Bold, Calm, Focus — each with background, text, secondary, link colors
  - Font size controls: increase/decrease with 12–30pt range, 1pt step
  - 7 font family options: System, Georgia, Palatino, New York, Athelas, Charter, Iowan Old Style
  - Line spacing: Compact (1.3x), Normal (1.6x), Loose (2.0x)
  - Margins: Narrow (16px), Normal (24px), Wide (36px)
  - Text alignment: Left or Justified
  - Settings panel UI (`mobile/lib/reader-settings-panel.tsx`): half-sheet modal with theme grid, font size row, customize section
  - Panel adapts to dark themes (Bold/Focus get dark panel styling)
  - `useReaderSettings()` hook: loads from SQLite on mount, persists every change
  - XHTML renderer updated: accepts fontFamily, lineHeight, textColor, linkColor, textAlign
  - Reader screen applies theme colors to header, footer, and background
  - Center tap zone (20%) toggles settings panel
  - Chapter re-renders on settings change (font, theme, spacing, margins, alignment)
  - TypeScript passes cleanly

### Completed (continued 3)
- [x] Controls overlay (top/bottom bars)
  - New `ControlsOverlay` component (`mobile/lib/controls-overlay.tsx`)
  - Top bar: "N pages left in chapter" text (matches Apple Books)
  - Bottom toolbar: frosted glass bar (`expo-blur` BlurView) with three buttons:
    - "Contents · N%" with list icon — placeholder for TOC (M4)
    - "Search Book" with magnifying glass icon — placeholder for search (M4)
    - "Themes & Settings" with Aa icon — opens existing settings panel
  - Bottom action row: frosted glass bar with Share, display, lock, bookmark icons
  - Smooth 200ms fade in/out animation via Reanimated `withTiming`
  - Center tap (20% zone) toggles controls overlay visibility
  - Controls auto-dismiss on page turn (tap or swipe) for uninterrupted reading
  - Theme-aware: adapts blur tint, icon colors, and toolbar background for dark/light themes
  - `isDark()` helper detects dark themes via luminance calculation
  - TOC list icon added to clean-state footer (bottom-right, matching Apple Books reference)
  - Ionicons added to reader screen for footer TOC icon
  - TypeScript passes cleanly, Expo build succeeds

### Completed (continued 4)
- [x] Progress tracking (local)
  - Reading position (chapter + page) saved to SQLite `reading_positions` table
  - Debounced save (1 second) on every page turn to avoid excessive writes
  - Position stored as JSON `{ chapter, page }` with calculated global page and progress percentage
  - Saved position restored on book reopen: loads chapter, scrolls to saved page after content measurement
  - Progress percentage calculated from global page counts across all chapters
  - Immediate save on reader unmount (cleanup timer + flush)
  - Uses UPSERT (`ON CONFLICT DO UPDATE`) for single-row-per-book semantics
  - TypeScript passes cleanly, Expo build succeeds

## M3 — Library & Sync

### Completed
- [x] Library grid with cover extraction
  - 2-column FlatList grid of book covers matching Apple Books layout
  - Books queried from SQLite with LEFT JOIN on `reading_positions` for progress
  - Sorted by most recently read (falls back to import date)
  - Cover extraction: parses EPUB, extracts cover image, caches as file in `Documents/covers/`
  - Covers saved on first load for books that have `epub_path` but no `cover_path`
  - `cover_path` updated in SQLite after extraction for instant display on subsequent loads
  - Placeholder covers: deterministic HSL color from title hash with title/author text overlay
  - 3D depth shadow on cover cards (offset shadow for physical book appearance)
  - Progress percentage shown below each cover (e.g. "42%")
  - Green "NEW" pill badge for books with no reading progress
  - Ellipsis icon for future context menu (long-press actions)
  - Pull-to-refresh to reload library
  - Reloads on tab focus (useFocusEffect) to pick up changes after reading
  - Empty state with library icon when no books exist
  - Tap navigates to reader screen via `router.push(/read/{id})`
  - Uses expo-file-system v19 new API (Paths, File, Directory classes)
  - TypeScript passes cleanly, Expo build succeeds

### Completed (continued)
- [x] Book import from document picker
  - New `book-import.ts` module (`mobile/lib/book-import.ts`) with full import pipeline
  - `importFromDocumentPicker()`: opens iOS/Android document picker filtered to `.epub` files
  - Copies selected EPUB to persistent `Documents/books/` directory
  - Parses EPUB metadata (title, author) via existing epub-parser
  - Extracts and caches cover image to `Documents/covers/`
  - Inserts book record into SQLite `books` table with UPSERT guard (prevents duplicates)
  - Background upload to backend R2 via presigned URL flow (POST → PUT → finalize)
  - ExpoFile used as Blob body for upload (memory-efficient, no full file buffering)
  - "+" button added to Library tab header (top-right, iOS convention)
  - "Import from Files" CTA button in empty library state with icon
  - Dependency: `expo-document-picker` installed
  - TypeScript passes cleanly

### Completed (continued 2)
- [x] Sync engine: push/pull with ReadingSync API
  - New `sync-engine.ts` module (`mobile/lib/sync-engine.ts`) with full push/pull sync
  - **Device identity**: generates and persists a unique device ID via `expo-secure-store`
  - **Push sync (enqueue + flush)**:
    - `enqueueSync()`: inserts progress into `sync_queue` table, coalesces pending entries per book
    - Debounced flush (3 seconds) — batches rapid page turns into one API call
    - `flushSyncQueue()`: processes pending queue items, POSTs to `POST /api/progress`
    - Handles "skipped" responses (server has newer timestamp) — marks as synced, no retry
    - Exponential retry on network errors, max 5 attempts before marking as failed
    - Auto-cleanup of old synced/failed entries (24h retention)
  - **Pull sync (on book open)**:
    - `fetchRemoteProgress()`: GETs latest progress from `GET /api/progress/:bookId`
    - `resolveProgressOnOpen()`: compares local vs remote, handles conflict resolution logic
    - Same-device detection: skips if remote is our own echo (device ID match)
    - Staleness threshold: ignores remote positions older than 7 days
    - Silent apply: if remote is <5% different from local, applies without prompting
    - Prompt action: if >5% different, returns "prompt" for future conflict UI
  - **Reader integration**:
    - `saveProgressToDb()` now enqueues sync after each local save
    - Non-blocking remote progress fetch on book open (background)
    - User navigation suppresses late-arriving sync prompts (`userHasNavigatedRef`)
    - Sync queue flushed on reader unmount (fire-and-forget)
    - `cancelPendingFlush()` called on cleanup to prevent stale timers
  - Offline-first: all reads work without network, syncs when connectivity returns
  - TypeScript passes cleanly, Expo build succeeds

### Completed (continued 3)
- [x] Excerpt-based text matching for Kindle sync
  - New `extractPlainText(epub, spineIndex)` function in `epub-parser.ts`: strips XHTML tags,
    decodes HTML entities, normalizes whitespace to produce searchable plain text
  - New `searchExcerpt(epub, excerpt, progressHint?)`: searches all spine chapters for an excerpt
    string, returns matches sorted by proximity to expected global progress position
  - New `extractExcerptAtPosition(epub, spineIndex, positionRatio)`: extracts ~500 chars of text
    at an approximate position within a chapter for cross-device sync payloads
  - Reader now populates `excerpt` field on every progress save — excerpt is stored in SQLite
    `reading_positions` table and included in sync pushes to the backend
  - Remote progress resolution now uses 3-tier strategy:
    1. Same-source mobile positions: parse `{ chapter, page }` JSON directly (existing behavior)
    2. Cross-renderer positions (Kindle): search EPUB for remote excerpt text, estimate page from
       character offset ratio — bypasses incompatible position formats entirely
    3. Percentage fallback: if excerpt not found, approximate position using `progress * spineLength`
  - Excerpt search normalizes whitespace on both sides for robust matching across renderers
  - Search is async and respects `userHasNavigatedRef` — late results are suppressed if user moved on
  - TypeScript passes cleanly

### Not Started

## M4 — Polish & Ship
Not started.
